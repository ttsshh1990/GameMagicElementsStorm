# Visuals

Visual helpers bridge configured runtime asset paths and lightweight Godot placeholders. Gameplay data remains in `resources/`; this directory only handles loading and fallback presentation.
