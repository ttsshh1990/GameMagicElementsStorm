# Meteor Fire Frames

Current runtime asset: 8 transparent PNG frames, `frame_000.png` through `frame_007.png`, each `256x256`.

Frames are loaded alphabetically and played once at 12 FPS by `skill_meteor_fire.tres`.
