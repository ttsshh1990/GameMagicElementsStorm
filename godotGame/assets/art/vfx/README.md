# VFX

Projectile and effect visual paths are configured in `resources/skills/*.tres`. Missing files are allowed during prototype work because combat scripts fall back to simple Godot-drawn placeholders.
