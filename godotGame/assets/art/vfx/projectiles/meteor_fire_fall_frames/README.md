# Meteor Fire Fall Frames

Staged falling meteor VFX frames for the fire-fire-fire meteor skill.

Current asset: 6 transparent PNG frames, `frame_000.png` through `frame_005.png`, each `192x192`.

These frames are not referenced by the current runtime yet. They are prepared for a future engineering pass that adds a falling meteor phase before the existing impact area VFX.
